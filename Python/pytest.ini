[pytest]
# Ensure this directory (Python source root) is on sys.path so "gafs" is found.
# Run pytest from this directory: python -m pytest gafs/...
pythonpath = .
log_cli = true
log_cli_level = DEBUG

# pytest-asyncio settings
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
